<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             Title ="KILOS">

    <ScrollView>
        <VerticalStackLayout
            Padding="20"
            Spacing="15">

            <Label Text="Set A Reminder"
                   FontSize="24"
                   HorizontalOptions="Center"
                />
            
            <!-- Reminder Title -->
            <Entry x:Name="ReminderTitle" 
                   Placeholder="Enter reminder Title"/>
            <!-- Reminder Date -->
            <DatePicker x:Name="ReminderDate" />
            <!-- Reminder Time -->
            <TimePicker x:Name="ReminderTime"/>
            
            <!-- Save Button -->
            
            <Button Text="Save Reminder" 
                    Clicked="OnSaveReminderClicked"
                    BackgroundColor="LightBlue"
                    TextColor="Black"/>
            
            <!-- Display Saved Reminder -->
            <Label x:Name="ReminderOutput" 
                   FontSize="18"
                   TextColor="DarkGreen"
                   HorizontalOptions="Center"/>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
